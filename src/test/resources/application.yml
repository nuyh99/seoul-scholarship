spring:
  application:
    name: morlatjanghak

  jpa:
    hibernate:
      ddl-auto: update

  datasource:
    url: jdbc:h2:mem:testdb;MODE=MYSQL;
    username: sa
    password: password
    driverClassName: org.h2.Driver
